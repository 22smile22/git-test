"""
Первые три команды пишутся только один раз когда вы делаете первый пуш
git --version
git config --global user.name "YOUR USERNAME ON GITHUB"
git config --global user.email "YOUR EMAIL ON GITHUB"
*
git init (для нового проекта)
git add .
git status

git remote add origin <HTTPS link.git> (Ремоте тоже пишется единожды когда соединяем проект с репозиторием)
git branch


"""